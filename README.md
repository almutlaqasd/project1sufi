# MainProject

ASP.NET Core MVC incident registration application.

## Local configuration

Copy `appsettings.example.json` to `appsettings.json` and replace the example connection string with your local database settings.
